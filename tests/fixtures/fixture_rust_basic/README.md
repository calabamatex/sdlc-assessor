# fixture_rust_basic

Clean Rust fixture: a tiny library using `Result` and `?` propagation. No unsafe, no `.unwrap()`, no `panic!`. Used to verify the Rust pack doesn't false-positive on idiomatic code.
