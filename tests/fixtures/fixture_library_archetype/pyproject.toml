[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "library-fixture"
version = "0.1.0"
description = "Library archetype fixture: pyproject with name+version and no scripts."
requires-python = ">=3.12"
