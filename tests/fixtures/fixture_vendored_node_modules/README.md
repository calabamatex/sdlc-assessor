# fixture_vendored_node_modules

Project with a vendored `node_modules/leftpad/` directory containing a fake `API_KEY = "x"` literal. Used to verify that detectors (SDLC-021) skip vendored dependencies and do not report the secret as a finding.
