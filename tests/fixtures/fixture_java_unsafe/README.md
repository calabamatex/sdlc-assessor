# fixture_java_unsafe

Dirty Java fixture exercising SDLC-056: `Runtime.getRuntime().exec`, `Class.forName`, `System.out.println`, `printStackTrace`, empty catch, `Thread.sleep`, and a `TODO` comment.
