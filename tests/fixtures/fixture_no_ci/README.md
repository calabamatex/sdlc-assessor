# fixture_no_ci

Production-shaped Python project with README and tests but no `.github/workflows/` directory. Used to exercise the `missing_ci_is_blocker` rule and the `missing_tests_and_missing_ci_can_trigger_blocker` rule (when test count is zero).
