[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "no-ci-fixture"
version = "0.1.0"
description = "Production-shaped fixture without CI."
requires-python = ">=3.12"
