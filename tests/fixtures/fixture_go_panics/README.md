# fixture_go_panics

Dirty Go fixture exercising the full SDLC-045 rule set: `panic()`, `unsafe.Pointer`, `exec.Command("sh", "-c", ...)`, `fmt.Println`, `recover()`, `init()` side effects, and a `TODO` comment.
